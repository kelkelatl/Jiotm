# Sets the flag to use modern AndroidX libraries, which is required
# because your build.gradle.kts includes libraries like androidx.core.
android.useAndroidX=true

# Optional: You can also add this flag to ensure any legacy support libraries
# are automatically migrated to their AndroidX equivalents.
android.enableJetifier=true
